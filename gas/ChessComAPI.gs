/**
 * Chess.com API interaction layer.
 */
var ChessComAPI = (function () {
  function fetchMonthlyArchive(username, year, month) {
    return [];
  }
  function fetchGamePgn(url) {
    return "";
  }
  return { fetchMonthlyArchive: fetchMonthlyArchive, fetchGamePgn: fetchGamePgn };
})();
